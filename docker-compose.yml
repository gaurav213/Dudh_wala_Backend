services:
  db:
    image: postgres:16-alpine
    container_name: doodh-khata-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: doodh_khata
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d doodh_khata']
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: doodh-khata-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Local compose stays development so placeholder secrets are allowed.
      # For a prod-like container, set NODE_ENV=production and strong JWT_* + CORS_ORIGINS.
      NODE_ENV: ${NODE_ENV:-development}
      PORT: 3000
      API_PREFIX: api
      API_VERSION: 1
      DATABASE_URL: postgresql://postgres:postgres@db:5432/doodh_khata
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-local_dev_access_secret_min_32_chars!!}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-local_dev_refresh_secret_min_32_chars!}
      JWT_ACCESS_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 30d
      BCRYPT_ROUNDS: 12
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173}
      DEFAULT_TIMEZONE: Asia/Kolkata
      LOG_LEVEL: info
      AUTO_APPROVE_FARMS: ${AUTO_APPROVE_FARMS:-true}
    ports:
      - '3000:3000'
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '-qO-',
          'http://localhost:3000/api/v1/health',
        ]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
